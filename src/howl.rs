pub fn howl(
    broker: String,
    topic_prefix: String,
    events_per_message: u32,
    messages_per_frame: u32,
    frames_per_second: u32,
    frames_per_run: u32,
    tof_peak: u32,
    tof_sigma: u32,
    det_min: u32,
    det_max: u32,
) {
    
}
